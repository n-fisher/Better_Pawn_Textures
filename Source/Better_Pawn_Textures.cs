using System.Collections.Generic;
using Verse;
using Harmony;

namespace Better_Pawn_Textures
{
    public class Better_Pawn_Textures : Mod
    {
        public Better_Pawn_Textures(ModContentPack content) : base(content)
        {
            List<PawnKindDef> pawnKinds = ;

            foreach (PawnKindDef kind in pawnKinds) {
                foreach ;
            }
        }
    }

    public class PawnGeneratorPatch
    {
        HarmonyInstance harmony = HarmonyInstance.Create("com.fyarn.")
    }
}